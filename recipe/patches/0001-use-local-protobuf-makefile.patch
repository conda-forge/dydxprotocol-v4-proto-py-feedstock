--- Makefile.orig   2024-07-19 14:52:16.008520276 -0500
+++ Makefile        2024-07-19 14:54:03.834496484 -0500
@@ -1,11 +1,6 @@
-DOCKER := $(shell which docker)
-protoVer=0.14.0
-protoImageName=ghcr.io/cosmos/proto-builder:$(protoVer)
-protoImage=$(DOCKER) run --rm -v $(CURDIR):/workspace --workdir /workspace $(protoImageName)
-
 proto-format:
-	@$(protoImage) find ./proto -name "*.proto" -exec clang-format -i {} \;
+	@find ./proto -name "*.proto" -exec clang-format -i {} \;
 
 proto-lint:
-	@$(protoImage) buf lint --error-format=json
+	@buf lint --error-format=json
 
@@ -13,3 +8,3 @@
 	@echo "Generating Protobuf files"
-	@$(protoImage) sh ./protocol/scripts/protocgen.sh
+	@sh ./protocol/scripts/protocgen.sh
 
@@ -17,3 +12,3 @@
 	@rm -rf ./.proto-export
-	@$(protoImage) buf breaking --against .git#branch=$$(git merge-base HEAD origin/main)
+	@buf breaking --against .git#branch=$$(git merge-base HEAD origin/main)
 
@@ -27,2 +22,6 @@
 PROTO_DIRS=$(shell find .proto-export-deps -path -prune -o -name '*.proto' -print0 | xargs -0 -n1 dirname | sort | uniq)
+PROTO_PACKAGES = amino cosmos cosmos_proto gogoproto tendermint
+GOOGLE_API_PACKAGES = api
+PROTO_FOLD_PREFIX = dydx_v4_ns_
+PROTO_FILE_PREFIX = dydx_v4_
 
@@ -32,13 +31,57 @@
 v4-proto-py-gen: proto-export-deps
+	@# Copy and modify each proto file
+	@for file in $$(find .proto-export-deps -name '*.proto' | sort -u); do \
+  		sed -i '/^[[:space:]]*\/\//d' $$file; \
+		rel_path=$$(echo $$file | sed 's|^.proto-export-deps/||'); \
+		protofile_name=$$(basename "$${file}"); \
+		target_dir=$$(dirname ".proto-export-deps-modified/$(PROTO_FOLD_PREFIX)$$rel_path"); \
+		target_proto="$(PROTO_FILE_PREFIX)$$protofile_name"; \
+		case "$$rel_path" in \
+			*"dydxprotocol"*) \
+				target_dir=$$(dirname ".proto-export-deps-modified/$$rel_path"); \
+				target_proto=$$protofile_name;; \
+		esac; \
+		mkdir -p "$$target_dir"; \
+		sed 's|google\.protobuf\.|.google.protobuf.|g' "$$file" > "$$target_dir/$$target_proto"; \
+		${PYTHON} ${RECIPE_DIR}/patches/update_packages.py \
+			--packages $$(echo "$(PROTO_PACKAGES) google" | tr ' ' ',') \
+			--proto_file_prefix $(PROTO_FILE_PREFIX) \
+			--proto_fold_prefix $(PROTO_FOLD_PREFIX) \
+			--file "$$target_dir/$$target_proto"; \
+		${PYTHON} ${RECIPE_DIR}/patches/update_extensions.py \
+			--prefix $(PROTO_FILE_PREFIX) \
+			--proto_file $$(echo "$$protofile_name" | sed 's|\.proto\b||') \
+			--map_file extensions_map.json \
+			"$$target_dir/$$target_proto" "$$target_dir/$$target_proto"; \
+	done
+	@# We need 2 passes to fix imports because we can't guarantee the order of the files
+	@echo "Modifying protos..."
+	@for file in $$(find .proto-export-deps -name '*.proto' | grep -v 'dydxprotocol' | sort -u); do \
+		rel_path=$$(dirname $$(echo $$file | sed 's|^.proto-export-deps/||')); \
+		source_proto_imp="$$rel_path/$$(basename $$file)"; \
+		target_proto_imp="$$rel_path/$(PROTO_FILE_PREFIX)$$(basename $$file)"; \
+		source_proto_opt=$$(echo "$$source_proto_imp" | tr / . | sed 's|\.proto\b||'); \
+		target_proto_opt=$$(echo "$$target_proto_imp" | tr / . | sed 's|\.proto\b||'); \
+	    find .proto-export-deps-modified -name '*.proto' -exec sed -i \
+	    	-e "s|$$source_proto_imp|$$target_proto_imp|g" \
+	    	-e "s|$$source_proto_opt|$$target_proto_opt|g" \
+	    	{} \; ; \
+	done
+	@${PYTHON} ${RECIPE_DIR}/patches/update_extensions_usage.py \
+		--mapping extensions_map.json \
+		--dir .proto-export-deps-modified \
+		--prefix $(PROTO_FILE_PREFIX)
 	@rm -rf ./v4-proto-py/v4_proto
 	@mkdir -p ./v4-proto-py/v4_proto
-	@for dir in $(PROTO_DIRS); do \
-		python3 -m grpc_tools.protoc \
-		-I .proto-export-deps \
-		--python_out=./v4-proto-py/v4_proto \
-		--pyi_out=./v4-proto-py/v4_proto \
-		--grpc_python_out=./v4-proto-py/v4_proto \
-		$$(find ./$${dir} -type f -name '*.proto'); \
-	done;
-	perl -i -pe 's/^from (?!google\.protobuf)([^ ]*) import ([^ ]*)_pb2 as ([^ ]*)$$/from v4_proto.\1 import \2_pb2 as \3/' $$(find ./v4-proto-py/v4_proto -type f \( -name '*_pb2.py' -o -name '*_pb2_grpc.py' -o -name '*_pb2.pyi' -o -name '*_pb2_grpc.pyi' \))
+	@echo "Compiling protos..."
+	@(cd .proto-export-deps-modified && ${PYTHON} -m grpc_tools.protoc \
+		-I . \
+		--python_out=../v4-proto-py/v4_proto \
+		--pyi_out=../v4-proto-py/v4_proto \
+		--grpc_python_out=../v4-proto-py/v4_proto \
+		$$(find . -name '*.proto'))
+	@perl -i -pe 's/^from (?!google\.protobuf)([^ ]*) import ([^ ]*)_pb2 as ([^ ]*)$$/from v4_proto.\1 import \2_pb2 as \3/' $$(find ./v4-proto-py/v4_proto -type f \( -name '*_pb2.py' -o -name '*_pb2_grpc.py' -o -name '*_pb2.pyi' -o -name '*_pb2_grpc.pyi' \))
+	@(cd ./v4-proto-py && ${PYTHON} ${RECIPE_DIR}/patches/mirror_python_tree.py --source v4_proto --proto_fold_prefix $(PROTO_FOLD_PREFIX) --proto_file_prefix $(PROTO_FILE_PREFIX))
+	@find ./v4-proto-py/v4_proto -type d -exec touch {}/__init__.py \;
+	@find ./v4-proto-py/v4_proto -type f -name '*.bak' -delete || true
 
